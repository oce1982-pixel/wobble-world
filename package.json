{
  "name": "wobble-world",
  "version": "1.0.0",
  "private": true,
  "description": "Hatch, collect and play with wobbly creatures.",
  "scripts": {
    "build": "node tools/check.js && node build.js",
    "start": "npx serve www",
    "screenshots": "node store/shoot.js",
    "icons": "node icons/make-icons.js",
    "sync": "npm run build && npx cap sync",
    "ios": "npm run sync && npx cap open ios",
    "android": "npm run sync && npx cap open android",
    "check": "node tools/check.js",
    "smoke": "node build.js && node tools/smoke.js",
    "verify": "node tools/check.js && node build.js && node tools/smoke.js"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2",
    "@revenuecat/purchases-capacitor": "^13.6.0"
  }
}
